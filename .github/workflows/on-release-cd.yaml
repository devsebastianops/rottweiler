name: Release CD

permissions:
  contents: write
  pages: write
  id-token: write

on:
  release:
    types: [published]

jobs:
  docker:
    uses: devsebastianops/github-workflows/.github/workflows/release-docker.yaml@main
    secrets: inherit
    with:
      binary: "rottweiler"

  go:
    uses: devsebastianops/github-workflows/.github/workflows/release-go.yaml@main
    secrets: inherit
    with:
      binary: "rottweiler"
      

  github-pages:
    uses: devsebastianops/github-workflows/.github/workflows/release-github-pages.yaml@main
    secrets: inherit
    with:
      binary: "rottweiler"